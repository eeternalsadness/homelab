prometheus:
  enabled: true

  prometheusSpec:
    retention: 10d
    resources: {}

    # data persistence
    storageSpec:
      volumeClaimTemplate:
        spec:
          #storageClassName: default
          storageClassName: standard # minikube
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 10Gi
