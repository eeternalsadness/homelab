apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mcp-cert
  namespace: mcp-servers
spec:
  secretName: mcp-tls
  issuerRef:
    name: cluster-issuer
    kind: ClusterIssuer
  dnsNames:
    - mcp.homelab.eeternalsadness.dev
