grafana:
  defaultDashboardsTimezone: Asia/Bangkok
  # FIXME: store this in vault somehow
  adminPassword: admin

  ingress:
    enabled: true
    ingressClassName: nginx # WARN: minikube only
    #ingressClassName: traefik
    path: /grafana
